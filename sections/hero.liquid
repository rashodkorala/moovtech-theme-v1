{%- liquid
  assign overlay_opacity = section.settings.hero_overlay_opacity | default: 55
  assign overlay_decimal = overlay_opacity | times: 1.0 | divided_by: 100.0

  assign video_src = 'hero-bg.mp4' | asset_url
  if section.settings.hero_video_mp4 != blank
    assign video_src = section.settings.hero_video_mp4
  endif

  assign hero_social_proof = section.settings.hero_social_proof
  if hero_social_proof == blank
    assign hero_social_proof = 'store_marketing.hero_social_proof' | t
  endif

  assign hero_eyebrow = section.settings.hero_eyebrow
  if hero_eyebrow == blank
    assign hero_eyebrow = 'store_marketing.hero_eyebrow' | t
  endif

  assign hero_headline = section.settings.hero_headline
  if hero_headline == blank
    assign hero_headline = 'store_marketing.hero_heading' | t
  endif

  assign hero_subheadline = section.settings.hero_subheadline
  if hero_subheadline == blank
    assign hero_subheadline = 'store_marketing.hero_subheading' | t
  endif

  assign cta_primary_label = section.settings.cta_primary_label
  if cta_primary_label == blank
    assign cta_primary_label = 'store_marketing.hero_cta_primary' | t
  endif

  assign cta_ghost_label = section.settings.cta_ghost_label
  if cta_ghost_label == blank
    assign cta_ghost_label = 'store_marketing.hero_cta_secondary' | t
  endif

  assign hero_micro_trust = section.settings.hero_micro_trust
  if hero_micro_trust == blank
    assign hero_micro_trust = 'store_marketing.hero_micro_trust' | t
  endif
-%}

<section class="moov-hero full-width" style="--hero-overlay: rgba(10, 10, 9, {{ overlay_decimal }});">
  {%- comment -%} Layer 1 — Video background {%- endcomment -%}
  <video
    class="moov-hero__video"
    autoplay
    muted
    loop
    playsinline
    preload="auto"
    {%- if section.settings.hero_poster -%}
      poster="{{ section.settings.hero_poster | image_url: width: 1920 }}"
    {%- endif -%}
    aria-hidden="true"
  >
    <source src="{{ video_src }}" type="video/mp4">
    {%- if section.settings.hero_video_webm != blank -%}
      <source src="{{ section.settings.hero_video_webm }}" type="video/webm">
    {%- endif -%}
  </video>

  {%- comment -%} Layer 2 — Dark overlay {%- endcomment -%}
  <div class="moov-hero__overlay" aria-hidden="true"></div>

  {%- comment -%} Layer 3 — Content {%- endcomment -%}
  <div class="moov-hero__content">
    {%- if section.settings.show_social_proof and hero_social_proof != blank -%}
      <p class="moov-hero__social-proof">{{ hero_social_proof | escape }}</p>
    {%- endif -%}

    {%- if hero_eyebrow != blank -%}
      <p class="moov-hero__eyebrow">{{ hero_eyebrow | escape }}</p>
    {%- endif -%}

    <h1 class="moov-hero__heading">
      {{- hero_headline | escape -}}
    </h1>

    {%- if hero_subheadline != blank -%}
      <p class="moov-hero__subheadline">{{ hero_subheadline | escape }}</p>
    {%- endif -%}

    <div class="moov-hero__ctas">
      {%- if cta_primary_label != blank -%}
        <a
          href="{{ section.settings.cta_primary_url | default: routes.root_url }}"
          class="moov-hero__btn moov-hero__btn--primary"
        >
          {{- cta_primary_label | escape -}}
        </a>
      {%- endif -%}

      {%- if cta_ghost_label != blank -%}
        <a
          href="{{ section.settings.cta_ghost_url | default: '#' }}"
          class="moov-hero__btn moov-hero__btn--ghost"
        >
          {{- cta_ghost_label | escape -}}
        </a>
      {%- endif -%}
    </div>

    {%- if hero_micro_trust != blank -%}
      <p class="moov-hero__micro-trust">{{ hero_micro_trust | escape }}</p>
    {%- endif -%}
  </div>
</section>

{% stylesheet %}
  .moov-hero {
    position: relative;
    min-height: 100vh;
    overflow: hidden;
    display: flex;
    align-items: center;
    background: var(--moov-black);
  }

  /* Layer 1 — Video / poster */
  .moov-hero__video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    z-index: 0;
  }

  /* Layer 2 — Overlay */
  .moov-hero__overlay {
    position: absolute;
    inset: 0;
    background: var(--hero-overlay, rgba(10, 10, 9, 0.55));
    z-index: 1;
  }

  /* Layer 3 — Content */
  .moov-hero__content {
    position: relative;
    z-index: 2;
    padding: 0 var(--section-inline-pad);
    max-width: 700px;
  }

  .moov-hero__social-proof {
    font-family: var(--font-body);
    font-weight: 400;
    font-size: var(--text-xs);
    color: var(--moov-text-secondary-dark);
    letter-spacing: 0.02em;
    margin: 0 0 clamp(10px, 1.2vw, 16px);
  }

  .moov-hero__eyebrow {
    font-family: var(--font-body);
    font-weight: 400;
    font-size: var(--text-2xs);
    color: rgba(240, 180, 41, 0.7);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    margin: 0 0 clamp(12px, 1.5vw, 20px);
  }

  .moov-hero__heading {
    font-family: var(--font-display);
    font-weight: 400;
    font-size: var(--text-5xl);
    line-height: 1.1;
    color: var(--moov-text-primary-dark);
    margin: 0 0 clamp(20px, 2.5vw, 32px);
  }

  .moov-hero__subheadline {
    font-family: var(--font-body);
    font-weight: 300;
    font-size: var(--text-lg);
    line-height: 1.7;
    color: var(--moov-text-secondary-dark);
    margin: 0 0 clamp(24px, 3vw, 40px);
    max-width: 520px;
  }

  /* CTAs */
  .moov-hero__ctas {
    display: flex;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
  }

  .moov-hero__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    padding: 14px 28px;
    font-family: var(--font-body);
    font-size: var(--text-sm);
    letter-spacing: 0.03em;
    text-decoration: none;
    white-space: nowrap;
  }

  .moov-hero__btn--primary {
    background: transparent;
    border: 0.5px solid var(--moov-border-amber);
    color: var(--moov-amber);
    font-weight: 400;
    transition: background 0.2s ease, color 0.2s ease;
  }

  .moov-hero__btn--primary:hover {
    background: var(--moov-amber);
    color: var(--moov-black);
  }

  .moov-hero__btn--ghost {
    background: transparent;
    border: 0.5px solid rgba(245, 238, 220, 0.20);
    color: var(--moov-text-secondary-dark);
    font-weight: 300;
    transition: border-color 0.2s ease, color 0.2s ease;
  }

  .moov-hero__btn--ghost:hover {
    border-color: rgba(245, 238, 220, 0.55);
    color: var(--moov-text-primary-dark);
  }

  .moov-hero__micro-trust {
    font-family: var(--font-body);
    font-weight: 300;
    font-size: var(--text-xs);
    line-height: 1.5;
    color: var(--moov-text-tertiary-dark);
    margin: clamp(16px, 2vw, 24px) 0 0;
    max-width: 420px;
  }

  /* Responsive */
  @media (max-width: 767px) {
    .moov-hero__overlay {
      background: rgba(10, 10, 9, 0.60);
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "MOOV Hero",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Video background"
    },
    {
      "type": "text",
      "id": "hero_video_mp4",
      "label": "Hero video URL (MP4)",
      "info": "Upload your video to Shopify Files and paste the URL here."
    },
    {
      "type": "text",
      "id": "hero_video_webm",
      "label": "Hero video URL (WebM)",
      "info": "Optional but recommended for smaller file size on Chrome and Firefox."
    },
    {
      "type": "image_picker",
      "id": "hero_poster",
      "label": "Hero video poster image",
      "info": "Shown before the video loads and on devices where autoplay is blocked."
    },
    {
      "type": "range",
      "id": "hero_overlay_opacity",
      "label": "Dark overlay opacity",
      "min": 20,
      "max": 80,
      "step": 5,
      "default": 55,
      "unit": "%",
      "info": "Controls how dark the overlay sits over the video. Higher = more readable text, less visible video."
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "checkbox",
      "id": "show_social_proof",
      "label": "Show social proof line",
      "default": true
    },
    {
      "type": "text",
      "id": "hero_social_proof",
      "label": "Social proof line",
      "info": "Leave blank to use theme translation (store_marketing.hero_social_proof)."
    },
    {
      "type": "text",
      "id": "hero_eyebrow",
      "label": "Eyebrow text",
      "default": "Morning wellness, engineered"
    },
    {
      "type": "text",
      "id": "hero_headline",
      "label": "Headline",
      "default": "Lead the first hour of your day."
    },
    {
      "type": "textarea",
      "id": "hero_subheadline",
      "label": "Subheadline",
      "default": "MOOV combines a wall-mounted alarm you physically disarm, a companion app that shields distracting apps at wake, and short routines that adapt to your goals—so you start clear, not reactive."
    },
    {
      "type": "header",
      "content": "Primary CTA"
    },
    {
      "type": "text",
      "id": "cta_primary_label",
      "label": "Primary CTA label",
      "default": "Reserve your MOOV"
    },
    {
      "type": "url",
      "id": "cta_primary_url",
      "label": "Primary CTA link",
      "info": "Link to your pre-order product page, e.g. /products/moov-alarm"
    },
    {
      "type": "header",
      "content": "Ghost CTA"
    },
    {
      "type": "text",
      "id": "cta_ghost_label",
      "label": "Ghost CTA label",
      "default": "See how it works"
    },
    {
      "type": "url",
      "id": "cta_ghost_url",
      "label": "Ghost CTA link",
      "info": "Link to an anchor or page, e.g. /pages/how-it-works"
    },
    {
      "type": "textarea",
      "id": "hero_micro_trust",
      "label": "Trust line under CTAs",
      "info": "Leave blank for store_marketing.hero_micro_trust"
    }
  ],
  "presets": [
    {
      "name": "MOOV Hero"
    }
  ]
}
{% endschema %}
