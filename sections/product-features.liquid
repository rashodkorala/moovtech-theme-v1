<div class="product-features full-width">
  <div class="product-features__inner">

    {%- if section.settings.eyebrow != blank -%}
      <p class="product-features__eyebrow">{{ section.settings.eyebrow | escape }}</p>
    {%- endif -%}

    <div class="product-features__grid">
      {%- for block in section.blocks -%}
        {%- if block.type == 'feature' -%}
          <div
            class="product-features__item"
            data-animate="feat-scroll"
            style="--feat-delay: {{ forloop.index0 | times: 80 }}ms;"
            {{ block.shopify_attributes }}
          >
            {%- if block.settings.number != blank -%}
              <span class="product-features__num">{{ block.settings.number | escape }}</span>
            {%- endif -%}
            {%- if block.settings.title != blank -%}
              <h3 class="product-features__title">{{ block.settings.title | escape }}</h3>
            {%- endif -%}
            {%- if block.settings.body != blank -%}
              <p class="product-features__body">{{ block.settings.body | escape }}</p>
            {%- endif -%}
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>

  </div>
</div>

{% stylesheet %}
  .product-features {
    background: var(--moov-black);
    padding: var(--section-block-pad-y) var(--section-inline-pad);
  }

  .product-features__inner {
    max-width: 1200px;
    margin: 0 auto;
  }

  .product-features__eyebrow {
    font-family: var(--font-body);
    font-size: var(--text-xs);
    font-weight: 400;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--moov-eyebrow-accent);
    margin: 0 0 64px;
  }

  .product-features__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 48px;
  }

  .product-features__item {
    border-top: 1px solid var(--moov-surface-dark-divider);
    padding-top: 24px;
  }

  .product-features__num {
    display: block;
    font-family: var(--font-body);
    font-size: var(--text-xs);
    font-weight: 300;
    text-transform: uppercase;
    color: var(--moov-text-tertiary-dark);
  }

  .product-features__title {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 400;
    color: var(--moov-text-primary-dark);
    line-height: 1.2;
    margin: 12px 0 0;
  }

  .product-features__body {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-weight: 200;
    line-height: 1.7;
    color: var(--moov-text-secondary-dark);
    margin: 10px 0 0;
  }

  /* Scroll animation */
  [data-animate="feat-scroll"] {
    opacity: 0;
    transform: translateY(16px);
    transition: opacity 0.7s ease-out, transform 0.7s ease-out;
    transition-delay: var(--feat-delay, 0ms);
  }

  [data-animate="feat-scroll"].is-visible {
    opacity: 1;
    transform: none;
    transition-delay: 0s;
  }

  /* Tablet */
  @media (min-width: 768px) and (max-width: 1279px) {
    .product-features__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 40px;
    }
  }

  /* Mobile */
  @media (max-width: 767px) {
    .product-features__grid {
      grid-template-columns: 1fr;
      gap: 32px;
    }

    .product-features__eyebrow {
      margin-bottom: 40px;
    }
  }
{% endstylesheet %}

{% javascript %}
  (function () {
    var items = document.querySelectorAll('.product-features [data-animate="feat-scroll"]');
    if (!items.length) return;

    var observer = new IntersectionObserver(
      function (entries) {
        entries.forEach(function (entry) {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.1 }
    );

    items.forEach(function (item) { observer.observe(item); });
  })();
{% endjavascript %}

{% schema %}
{
  "name": "t:sections.product_features.name",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "t:labels.eyebrow_text",
      "default": "What it does"
    }
  ],
  "blocks": [
    {
      "type": "feature",
      "name": "t:sections.product_features.blocks.feature.name",
      "settings": [
        {
          "type": "text",
          "id": "number",
          "label": "t:labels.feature_number",
          "default": "01"
        },
        {
          "type": "text",
          "id": "title",
          "label": "t:labels.heading",
          "default": "Feature title"
        },
        {
          "type": "textarea",
          "id": "body",
          "label": "t:labels.body",
          "default": "Describe this feature in one or two clear sentences."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.product_features.name",
      "blocks": [
        { "type": "feature", "settings": { "number": "01", "title": "Motion-activated dismissal", "body": "You can't tap it half-asleep. MOOV requires a deliberate physical movement to go silent. One action, one snooze loop broken." } },
        { "type": "feature", "settings": { "number": "02", "title": "Up to 24-hour battery", "body": "A full day on a single charge. Plug in before bed and wake up to a full battery every morning. USB-C included." } },
        { "type": "feature", "settings": { "number": "03", "title": "Bluetooth sync", "body": "Pairs instantly with the MOOV app on iOS and Android. Set your alarm, configure your routine, and manage app blocking from your phone." } },
        { "type": "feature", "settings": { "number": "04", "title": "App-controlled routines", "body": "The app builds a short morning routine around your actual schedule and energy. Not a fixed protocol. A living routine that adapts to your day." } }
      ]
    }
  ]
}
{% endschema %}
